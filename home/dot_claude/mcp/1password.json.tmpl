{
  "command": "bash",
  "args": [
    "-c",
    "set -a && . $HOME/.secrets && set +a && exec {{ .chezmoi.homeDir }}/.local/node/bin/op-mcp"
  ],
  "env": {
    "OP_PATH": "{{ .chezmoi.homeDir }}/.local/bin/op"
  }
}
